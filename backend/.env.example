# === MONGODB ===
MONGO_URL=mongodb+srv://...
DB_NAME=infinea

# === JWT ===
JWT_SECRET=change-this-in-production

# === AI (Anthropic Claude) ===
ANTHROPIC_API_KEY=sk-ant-...

# === STRIPE (paiements) ===
STRIPE_API_KEY=sk_test_...

# === GOOGLE OAUTH (login + calendar) ===
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# === NOTION ===
NOTION_CLIENT_ID=your_notion_oauth_client_id
NOTION_CLIENT_SECRET=your_notion_oauth_client_secret
# Redirect URI: {BACKEND_URL}/api/integrations/callback/notion

# === TODOIST ===
TODOIST_CLIENT_ID=your_todoist_client_id
TODOIST_CLIENT_SECRET=your_todoist_client_secret
# Redirect URI: {BACKEND_URL}/api/integrations/callback/todoist

# === SLACK ===
SLACK_CLIENT_ID=your_slack_client_id
SLACK_CLIENT_SECRET=your_slack_client_secret
# Redirect URI: {BACKEND_URL}/api/integrations/callback/slack

# === iCal ===
# No credentials needed — iCal uses URL-based connection

# === URLS (pour le flux OAuth) ===
BACKEND_URL=https://infinea-api.onrender.com
FRONTEND_URL=https://infinea.vercel.app

# === CORS ===
ALLOWED_ORIGINS=https://infinea.vercel.app,http://localhost:3000

# === PROMO CODE (admin-only premium activation) ===
ADMIN_EMAILS=admin@infinea.com
# Generate hash: python -c "import bcrypt; print(bcrypt.hashpw(b'YOUR_CODE', bcrypt.gensalt()).decode())"
PROMO_CODE_HASH=
